<div class="container">
  <h1>Add new movie to your </h1>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie, collection: %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>

</div>
